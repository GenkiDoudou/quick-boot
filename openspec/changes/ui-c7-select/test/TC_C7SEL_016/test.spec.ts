/**
 * 对应清单：TC_C7SEL_016 - 插槽 prefix / empty 透传至 ElSelect
 * 需 Dev 页或 Story 提供带插槽的 C7Select 实例。
 */
import {test} from '@playwright/test'

test.describe('TC_C7SEL_016 - 插槽 prefix / empty 透传至 ElSelect', () => {
  test('占位-待插槽示例', async () => {
    test.skip(true, '待 Dev 页增加带 prefix/empty 插槽示例后实现')
  })
})
