/**
 * 对应清单：TC_C7SEL_014 - fetchData reject 时保留已有选项与 loading 回落
 * 需可控 reject 的临时 fetch 或专用页。
 */
import {test} from '@playwright/test'

test.describe('TC_C7SEL_014 - fetchData reject 时保留已有选项与 loading 回落', () => {
  test('占位-待失败注入', async () => {
    test.skip(true, '待可控失败注入后再实现')
  })
})
