/**
 * 对应清单：TC_C7SEL_010 - 多选无 separator 对外数组
 * Dev 页未挂载专用示例，待补充页面后启用。
 */
import {test} from '@playwright/test'

test.describe('TC_C7SEL_010 - 多选无 separator 对外数组', () => {
  test('占位-待 Dev 分区', async () => {
    test.skip(true, '待 Dev 页增加 multiple 且无 separator 分区后实现')
  })
})
