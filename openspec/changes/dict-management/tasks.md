## 1. 数据库与领域模型落地

- [x] 1.1 新增字典类型表与字典项表迁移脚本（含审计字段、逻辑删除、`status` 字段与必要索引）。
- [x] 1.2 增加字典类型实体、字典项实体与 Mapper，完成字段映射与 JavaDoc。
- [x] 1.3 落地唯一约束策略：字典类型唯一、字典项 `dictType + dictValue` 唯一。

## 2. 后端接口与业务校验

- [x] 2.1 实现字典类型接口：列表、详情、新增、修改、删除、导出、刷新缓存（单类型/全量）。
- [x] 2.2 实现字典项接口：按 `dictType` 列表、详情、新增、修改、删除、导出。
- [x] 2.3 写接口统一采用 `POST` 语义路径（含 `/update`、`/remove/{id}`）。
- [x] 2.4 增加 Validation 注解与 OpenAPI 注解，保证参数校验与文档一致。
- [x] 2.5 实现删除字典前置校验：存在未删除字典项时拒绝删除并返回可读业务错误。
- [x] 2.6 使用自定义异常替代 `IllegalArgumentException`，统一交由全局异常处理返回 `R.error(...)`。

## 3. 缓存与导出能力

- [x] 3.1 实现单类型缓存刷新能力并验证刷新后读取最新值。
- [x] 3.2 实现全量缓存刷新能力并验证批量刷新结果。
- [x] 3.3 实现字典类型列表导出与字典项按 `dictType` 导出。

## 4. 前端主从页面实现

- [x] 4.1 新增字典类型管理页，支持筛选、查看、新增、修改、删除、导出、刷新缓存、进入字典项页。
- [x] 4.2 新增字典项管理页，支持按 `dictType` 查询及查看、新增、修改、删除、导出。
- [x] 4.3 配置主从路由：`/system/dict/type` 与 `/system/dict/data/:dictType`。
- [x] 4.4 页面核心交互优先使用 `@/packages` 组件（`C7Button`、`C7Dialog`、`C7JsonTable`、`C7DictTag` 等）。
- [x] 4.5 配置并校验权限点：`system:dict:list/query/add/edit/remove/export/refresh`。
- [x] 4.6 字典类型/字典数据导入接入平台异步编排（`import-biz-type` + `import-force-async`，走 `ImportOrchestrator`）。

## 5. 测试与验收

- [ ] 5.1 增加后端测试覆盖：唯一性校验、删除受限、缓存刷新、接口响应契约。
- [ ] 5.2 完成前后端联调，验证主从跳转、CRUD、导出、刷新缓存全链路。
- [ ] 5.3 执行前端构建与后端模块级构建/测试，并记录阻塞项（如环境版本）与处理结果。
- [ ] 5.4 对照 `specs/dict-management/spec.md` 全量走查并更新任务勾选状态。

