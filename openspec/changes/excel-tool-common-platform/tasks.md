## 1. 公共模型与接口收敛

- [x] 1.1 在 `quickboot-common` 新增并整理 `excel/model` 下的导入上下文、策略枚举与结果模型（含失败明细字段）
- [x] 1.2 统一 `excel/listener` 接口与默认实现，明确 validation 与回调行为
- [x] 1.3 统一 `excel/exception` 字段级异常与业务校验异常语义

## 2. 导入执行模板实现

- [x] 2.1 在 `excel/core` 实现统一导入执行入口（回调模板）
- [x] 2.2 支持错误聚合与失败明细标准化输出（行号/列/原因）
- [x] 2.3 实现导入策略上下文透传（`IGNORE` / `OVERWRITE`）并保留业务自定义重复语义

## 3. 导出能力完善

- [x] 3.1 完善普通导出与模板导出统一入口
- [x] 3.2 接入字典翻译转换链路（导出与导入方向一致）
- [x] 3.3 完善合并单元格注解与策略接入能力

## 4. 字典模块样板接入

- [x] 4.1 将 `dict type` 导入改造为统一模板接入，保留业务层写库决策
- [x] 4.2 将 `dict data` 导入改造为统一模板接入，保留业务层写库决策
- [x] 4.3 统一错误列中文映射与失败文件返回字段，保证前端下载行为一致

## 5. 兼容性与回归验证

- [ ] 5.1 补充 `quickboot-common` 单元测试（监听器、校验、失败文件、策略分支）
- [ ] 5.2 补充字典模块集成测试（`IGNORE` / `OVERWRITE`、失败明细准确性）
- [ ] 5.3 验证现有导入导出接口兼容性，确认对外契约不回归

## 6. 迁移规范与落地说明

- [x] 6.1 编写后续业务模块接入指引（工具层职责与业务层职责边界）
- [x] 6.2 标记旧导入实现迁移路径并给出收敛计划
- [x] 6.3 在 OpenSpec 变更中补充实施备注与风险回退步骤

