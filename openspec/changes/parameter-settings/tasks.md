## 1. 后端接口与规则实现

- [x] 1.1 盘点并创建参数模块 DTO/VO，请求参数接入 Jakarta Validation，补齐 Swagger 注解（`@Tag`、`@Operation`、`@Parameter`）。
- [x] 1.2 实现参数分页查询与详情/按键名查询接口，确保筛选条件与返回字段符合规格。
- [x] 1.3 实现 `POST /system/config/create` 与 `POST /system/config/update`，在 Service 层落实 `configKey` 全局唯一校验。
- [x] 1.4 实现系统内置参数保护规则：禁止删除，且仅允许修改 `configValue`。
- [x] 1.5 实现 `POST /system/config/remove` 批量删除逻辑（仅允许删除非内置参数）。

## 2. 缓存刷新、导出与审计接入

- [x] 2.1 实现 `POST /system/config/refreshCache` 全量刷新接口，并返回可读中文结果。
- [x] 2.2 实现 `POST /system/config/export`，按当前筛选条件导出全量结果且字段与列表一致。
- [ ] 2.3 将新增、编辑、删除、刷新缓存操作接入系统操作日志，确认可追踪。
- [x] 2.4 统一业务失败异常与中文提示文案，避免使用 `IllegalArgumentException`。

## 3. 前端页面与交互实现

- [x] 3.1 新增参数设置页面查询区与表格区，支持参数名称/键名/内置标记/时间区间筛选、回车查询、重置、分页。
- [x] 3.2 实现新增/编辑弹窗表单，完成必填校验与提交后列表刷新。
- [x] 3.3 实现删除交互（含二次确认）与内置参数删除限制提示。
- [x] 3.4 实现导出按钮（按筛选导出）与全量刷新缓存按钮（同步等待+结果提示）。
- [x] 3.5 接入权限点 `system:config:list/add/edit/remove/export/query` 并隐藏无权限操作。

## 4. 联调与验收验证

- [ ] 4.1 完成前后端联调，验证新增/编辑/删除/查询/导出/刷新缓存主流程。
- [ ] 4.2 验证 `configKey` 重复拦截、内置参数删除拦截、内置参数字段编辑边界等关键规则。
- [ ] 4.3 执行后端模块级测试与前端构建验证，修复阻塞问题。
- [ ] 4.4 对照验收标准逐项走查，确认本次范围内条目全部达成并记录遗留项。
