/**
 * 会话 ID：委托 @quickboot/lite-rum（保持原导出路径兼容）。
 */
export {
  getOrCreateSessionId,
  resetSessionId,
  clearSessionId,
  onSessionContextChange,
  resetSessionContextForTest
} from '@quickboot/lite-rum'

import {
  getOrCreateSessionId,
  resetSessionId,
  clearSessionId,
  onSessionContextChange,
  resetSessionContextForTest
} from '@quickboot/lite-rum'

export default {
  getOrCreateSessionId,
  resetSessionId,
  clearSessionId,
  onSessionContextChange,
  resetSessionContextForTest
}
