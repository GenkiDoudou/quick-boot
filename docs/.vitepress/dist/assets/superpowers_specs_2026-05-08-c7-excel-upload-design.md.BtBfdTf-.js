import{_ as t,c as e,o as s,b2 as r}from"./chunks/framework.CUhoHPxN.js";const u=JSON.parse('{"title":"C7ExcelUpload（C7 Excel 导入）设计说明","description":"","frontmatter":{},"headers":[],"relativePath":"superpowers/specs/2026-05-08-c7-excel-upload-design.md","filePath":"superpowers/specs/2026-05-08-c7-excel-upload-design.md"}'),n={name:"superpowers/specs/2026-05-08-c7-excel-upload-design.md"};function d(a,o,g,c,l,i){return s(),e("div",null,[...o[0]||(o[0]=[r(`<h1 id="c7excelupload-c7-excel-导入-设计说明" tabindex="-1">C7ExcelUpload（C7 Excel 导入）设计说明 <a class="header-anchor vp-link" href="#c7excelupload-c7-excel-导入-设计说明" aria-label="Permalink to &quot;C7ExcelUpload（C7 Excel 导入）设计说明&quot;">​</a></h1><p><strong>日期</strong>：2026-05-08<br><strong>状态</strong>：已定稿（经 brainstorming 澄清 <strong>1A～7A</strong> 与「确认定稿」）<br><strong>依据</strong>：<code>原始需求/前端/C7Excel导入.md</code> + Q&amp;A 结论及实现路径 <strong>路径 1</strong>（单文件 <code>index.vue</code> + 必要时同文件内纯函数）</p><hr><h2 id="_1-背景与目标" tabindex="-1">1. 背景与目标 <a class="header-anchor vp-link" href="#_1-背景与目标" aria-label="Permalink to &quot;1. 背景与目标&quot;">​</a></h2><ul><li><strong>背景</strong>：Excel 导入需要统一的文件选择、类型/大小校验、导入策略选择（覆盖/忽略），并在导入完成后展示结果与错误明细下载入口。</li><li><strong>目标</strong>：提供 <strong><code>C7ExcelUpload</code></strong>：选择 <strong><code>.xls/.xlsx</code></strong>、选择 <strong><code>overwrite|ignore</code></strong>、调用业务 <strong><code>uploadFn(file, strategy)</code></strong>，展示 <strong><code>total/successCount/failCount</code></strong>，在 <strong><code>failCount&gt;0</code> 且 <code>errorFileUrl</code> 存在</strong> 时展示错误明细链接；支持 <strong><code>notify</code></strong> 替换默认 <strong><code>ElMessage</code></strong>；暴露 <strong><code>uploading</code></strong> 与 <strong><code>reset()</code></strong>；<strong><code>uploading</code></strong> 与 <strong><code>v-model:uploading</code></strong> 对齐 <strong><code>C7ExcelDownload</code></strong> 的 <strong><code>downloading</code></strong> 约定。</li><li><strong>非目标</strong>：不内置 <strong><code>ElDialog</code> / <code>ElCard</code></strong> 外壳（由业务布局）；不在组件内拼装 <strong><code>FormData</code></strong> 或约定 multipart 字段名（由 <strong><code>uploadFn</code></strong> 内完成）；错误明细链接<strong>不</strong>强制复用 <strong><code>C7ExcelDownload</code></strong> 的 Blob 链（普通 <strong><code>&lt;a href&gt;</code></strong> 打开/下载即可）。</li></ul><hr><h2 id="_2-命名、边界与落点" tabindex="-1">2. 命名、边界与落点 <a class="header-anchor vp-link" href="#_2-命名、边界与落点" aria-label="Permalink to &quot;2. 命名、边界与落点&quot;">​</a></h2><div class="vp-table"><table><thead><tr><th>项</th><th>约定</th></tr></thead><tbody><tr><td><strong>对外组件名</strong></td><td><strong><code>C7ExcelUpload</code></strong></td></tr><tr><td><strong>目录</strong></td><td><strong><code>quick-ui/src/packages/C7ExcelUpload/index.vue</code></strong></td></tr><tr><td><strong>实现形态</strong></td><td><strong>单文件组件</strong>；扩展名/大小校验等可在同文件底部以 <strong>纯函数</strong> 形式组织。首版不抽 <strong><code>useC7ExcelUpload</code></strong>。</td></tr><tr><td><strong>全局注册</strong></td><td>在 <strong><code>quick-ui/src/packages/index.js</code></strong> 中 <strong><code>export</code></strong> 与 <strong><code>installPackages</code></strong> 注册，与其它 C7 一致。</td></tr></tbody></table></div><p><strong>职责</strong>：文件选择与校验、策略展示与双向绑定、触发 <strong><code>uploadFn</code></strong>、管理 <strong><code>uploading</code></strong>、展示导入结果与错误文件入口、<strong><code>reset</code></strong>。</p><p><strong>不负责</strong>：接口 URL、鉴权、<strong><code>FormData</code></strong> 字段、后端 <strong><code>R</code></strong> 包装的解析（由 <strong><code>uploadFn</code></strong> 内整理为下文结果类型）。</p><hr><h2 id="_3-uploadfn-与结果类型-2a" tabindex="-1">3. <code>uploadFn</code> 与结果类型（<strong>2A</strong>） <a class="header-anchor vp-link" href="#_3-uploadfn-与结果类型-2a" aria-label="Permalink to &quot;3. \`uploadFn\` 与结果类型（**2A**）&quot;">​</a></h2><ul><li><strong><code>uploadFn: (file: File, strategy: &#39;overwrite&#39; \\| &#39;ignore&#39;) =&gt; Promise&lt;C7ExcelUploadResult&gt;</code></strong></li><li><strong><code>C7ExcelUploadResult</code></strong>（成功 resolve 值，字段与原始需求一致）：</li></ul><div class="language-ts vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes vitesse-light vitesse-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#A0ADA0;--shiki-dark:#758575DD;">/** 业务在 uploadFn 内从接口响应整理后 resolve */</span></span>
<span class="line"><span style="--shiki-light:#AB5959;--shiki-dark:#CB7676;">interface</span><span style="--shiki-light:#2E8F82;--shiki-dark:#5DA994;"> C7ExcelUploadResult</span><span style="--shiki-light:#999999;--shiki-dark:#666666;"> {</span></span>
<span class="line"><span style="--shiki-light:#B07D48;--shiki-dark:#BD976A;">  total</span><span style="--shiki-light:#999999;--shiki-dark:#666666;">: </span><span style="--shiki-light:#2E8F82;--shiki-dark:#5DA994;">number</span></span>
<span class="line"><span style="--shiki-light:#B07D48;--shiki-dark:#BD976A;">  successCount</span><span style="--shiki-light:#999999;--shiki-dark:#666666;">: </span><span style="--shiki-light:#2E8F82;--shiki-dark:#5DA994;">number</span></span>
<span class="line"><span style="--shiki-light:#B07D48;--shiki-dark:#BD976A;">  failCount</span><span style="--shiki-light:#999999;--shiki-dark:#666666;">: </span><span style="--shiki-light:#2E8F82;--shiki-dark:#5DA994;">number</span></span>
<span class="line"><span style="--shiki-light:#A0ADA0;--shiki-dark:#758575DD;">  /** failCount&gt;0 时可选，用于错误明细链接 */</span></span>
<span class="line"><span style="--shiki-light:#B07D48;--shiki-dark:#BD976A;">  errorFileUrl</span><span style="--shiki-light:#AB5959;--shiki-dark:#CB7676;">?</span><span style="--shiki-light:#999999;--shiki-dark:#666666;">: </span><span style="--shiki-light:#2E8F82;--shiki-dark:#5DA994;">string</span></span>
<span class="line"><span style="--shiki-light:#999999;--shiki-dark:#666666;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br></div></div><ul><li><strong><code>emit(&#39;success&#39;, result: C7ExcelUploadResult)</code></strong>：在 <strong><code>uploadFn</code> resolve</strong> 且组件完成结果状态更新后触发（与 <strong><code>revokeObjectURL</code></strong> 无关；本组件无 Blob 链）。</li></ul><hr><h2 id="_4-props、策略、通知" tabindex="-1">4. Props、策略、通知 <a class="header-anchor vp-link" href="#_4-props、策略、通知" aria-label="Permalink to &quot;4. Props、策略、通知&quot;">​</a></h2><div class="vp-table"><table><thead><tr><th>项</th><th>约定</th></tr></thead><tbody><tr><td><strong><code>accept</code></strong></td><td>默认 <strong><code>&#39;.xls,.xlsx&#39;</code></strong>（实现可与需求一致；若允许覆盖须保证默认仍满足验收「仅 xls/xlsx」语义）。</td></tr><tr><td><strong><code>maxSizeMb</code></strong></td><td><strong>必填</strong>，<code>number</code>，允许的最大文件大小（<strong>MB</strong>）；与原始需求 <strong><code>fileSize</code></strong> 对应。</td></tr><tr><td><strong><code>duplicateStrategy</code></strong></td><td><strong><code>&#39;overwrite&#39; | &#39;ignore&#39;</code></strong>，配合 <strong><code>update:duplicateStrategy</code></strong> 支持 <strong><code>v-model:duplicateStrategy</code></strong>；<strong>默认 <code>&#39;ignore&#39;</code></strong>（与 <strong><code>reset()</code></strong> 复位一致）。</td></tr><tr><td><strong><code>overwriteLabel</code> / <code>ignoreLabel</code></strong></td><td>可选；缺省时中文 <strong>「覆盖」/「忽略」</strong>（i18n 策略以实现/代码评审为准）。</td></tr><tr><td><strong><code>notify</code></strong></td><td><strong><code>(type: &#39;success&#39; | &#39;error&#39; | &#39;warning&#39; | &#39;info&#39;, message: string) =&gt; void</code></strong>，可选；未传时错误/校验提示用 <strong><code>ElMessage</code></strong>（<strong>5A</strong>）。</td></tr><tr><td><strong><code>uploadFn</code></strong></td><td>见第 3 节；缺失时实现可 <strong><code>console.warn</code> + no-op</strong>，以代码评审为准。</td></tr></tbody></table></div><p><strong>根节点与透传</strong>：触发导入、选文件等按钮建议使用 <strong><code>ElButton</code></strong>；常用展示属性通过 <strong><code>v-bind=&quot;$attrs&quot;</code></strong> 透传至主要按钮（与 <strong><code>C7ExcelDownload</code></strong> 同类约定；<strong><code>inheritAttrs: false</code></strong> 时在根按钮合并 <strong><code>class</code> / <code>style</code></strong>，避免重复绑定内部事件名——细节写入实现计划）。</p><hr><h2 id="_5-事件、状态与-reset-6a、7a" tabindex="-1">5. 事件、状态与 <code>reset</code>（<strong>6A、7A</strong>） <a class="header-anchor vp-link" href="#_5-事件、状态与-reset-6a、7a" aria-label="Permalink to &quot;5. 事件、状态与 \`reset\`（**6A、7A**）&quot;">​</a></h2><div class="vp-table"><table><thead><tr><th>项</th><th>约定</th></tr></thead><tbody><tr><td><strong><code>error</code></strong></td><td><strong><code>emit(&#39;error&#39;, err: unknown)</code></strong>：仅当 <strong><code>uploadFn</code> reject</strong>（或内部将其视为失败的异步错误）时触发。</td></tr><tr><td><strong>校验失败（扩展名/大小）</strong></td><td><strong><code>notify(&#39;error&#39;, message)</code></strong>；<strong>不</strong> <strong><code>emit(&#39;error&#39;)</code></strong>，避免与接口失败混用。</td></tr><tr><td><strong><code>uploading</code></strong></td><td>内部 <strong><code>ref(false)</code></strong>；在 <strong><code>uploadFn</code> 开始时</strong> 置 <strong><code>true</code></strong>，在 <strong><code>finally</code></strong> 置 <strong><code>false</code></strong>。</td></tr><tr><td><strong><code>v-model:uploading</code></strong></td><td><strong><code>uploading</code> prop + <code>update:uploading</code></strong>；组件内开始导入时 <strong><code>emit(&#39;update:uploading&#39;, true)</code></strong> 等与 Vue 3 习惯一致。</td></tr><tr><td><strong>防重复</strong></td><td><strong><code>uploading === true</code></strong> 时点击导入 <strong>no-op</strong>（<strong>7A</strong>）。</td></tr><tr><td><strong><code>defineExpose</code></strong></td><td><strong><code>{ uploading, reset }</code></strong>，其中 <strong><code>uploading</code></strong> 与内部状态一致（或只读 ref，以实现为准）。</td></tr><tr><td><strong><code>reset()</code></strong></td><td>清空已选文件展示、<strong>将 file input 的 <code>value</code> 置空</strong>（支持重复选择同一文件）、清空上次结果展示、<strong><code>duplicateStrategy</code> 复位为 <code>&#39;ignore&#39;</code></strong>（与默认一致）。</td></tr></tbody></table></div><hr><h2 id="_6-文件选择与校验" tabindex="-1">6. 文件选择与校验 <a class="header-anchor vp-link" href="#_6-文件选择与校验" aria-label="Permalink to &quot;6. 文件选择与校验&quot;">​</a></h2><ul><li>使用 <strong>隐藏 <code>input[type=&quot;file&quot;</code>]</strong> + 展示用 <strong><code>ElButton</code></strong>（或链接按钮）触发 <strong><code>click()</code></strong> 打开文件选择器。</li><li><strong>选择后</strong>：校验扩展名（在 <strong><code>accept</code></strong> 与业务约定下，仅允许 <strong><code>.xls</code> / <code>.xlsx</code></strong>）与 <strong>文件大小 ≤ <code>maxSizeMb</code></strong>（注意 <strong>MB → 字节</strong> 换算边界）。不通过则 <strong>第 5 节</strong> 校验失败路径；<strong>不调用</strong> <strong><code>uploadFn</code></strong>。</li><li><strong>每次选择并处理完 change 后</strong>：重置 <strong>input</strong> 的 <strong>value</strong>（原始需求：允许重复选择同一文件）。</li></ul><hr><h2 id="_7-导入结果与错误文件-3a" tabindex="-1">7. 导入结果与错误文件（<strong>3A</strong>） <a class="header-anchor vp-link" href="#_7-导入结果与错误文件-3a" aria-label="Permalink to &quot;7. 导入结果与错误文件（**3A**）&quot;">​</a></h2><ul><li>成功后在界面展示 <strong><code>total</code> / <code>successCount</code> / <code>failCount</code></strong>。</li><li>当 <strong><code>failCount &gt; 0</code></strong> 且 <strong><code>errorFileUrl</code></strong> 为<strong>非空字符串</strong>（trim 后）：展示错误明细入口，例如 <strong><code>&lt;a :href=&quot;errorFileUrl&quot; target=&quot;_blank&quot; rel=&quot;noopener noreferrer&quot;&gt;</code></strong>，文案如 <strong>「下载错误明细」</strong>（实现计划可微调）。</li><li><strong>不</strong>要求走 <strong><code>C7ExcelDownload</code></strong> 或 Blob 文件名解析；由服务端响应头/直链行为决定浏览器是下载还是预览。</li></ul><hr><h2 id="_8-ui-结构-无外壳-4a" tabindex="-1">8. UI 结构（无外壳 <strong>4A</strong>） <a class="header-anchor vp-link" href="#_8-ui-结构-无外壳-4a" aria-label="Permalink to &quot;8. UI 结构（无外壳 **4A**）&quot;">​</a></h2><p>建议自上而下：<strong>选文件</strong> → <strong>策略</strong>（<strong><code>ElRadioGroup</code></strong> 或 <strong><code>ElSegmented</code></strong>，实现计划择一）→ <strong>导入</strong> → <strong>结果区</strong>（无结果时可折叠或隐藏，实现计划写清）。</p><hr><h2 id="_9-验收与测试建议" tabindex="-1">9. 验收与测试建议 <a class="header-anchor vp-link" href="#_9-验收与测试建议" aria-label="Permalink to &quot;9. 验收与测试建议&quot;">​</a></h2><ul><li>非法扩展名或超出 <strong><code>maxSizeMb</code></strong>：<strong>阻止上传</strong>、有 <strong><code>notify</code></strong>、<strong>不调用</strong> <strong><code>uploadFn</code></strong>、<strong>不</strong> <strong><code>emit(&#39;error&#39;)</code></strong>。</li><li>合法文件： <strong><code>uploadFn</code></strong> 收到正确 <strong><code>File</code></strong> 与 <strong><code>strategy</code></strong>。</li><li>成功 resolve：统计展示正确；<strong><code>failCount&gt;0</code> 且有 <code>errorFileUrl</code></strong>：链接可见。</li><li><strong><code>uploadFn</code> reject</strong>：<strong><code>notify</code> + <code>emit(&#39;error&#39;)</code></strong>。</li><li><strong><code>uploading</code> 为 true</strong> 时再次点导入：<strong><code>uploadFn</code> 调用次数不增加</strong>。</li><li><strong><code>reset()</code></strong> 后可再次选同一文件；统计清空；策略为 <strong><code>ignore</code></strong>。</li></ul><hr><h2 id="_10-后续流程" tabindex="-1">10. 后续流程 <a class="header-anchor vp-link" href="#_10-后续流程" aria-label="Permalink to &quot;10. 后续流程&quot;">​</a></h2><ul><li>实现前：由 <strong><code>writing-plans</code></strong> 产出分步实现计划（含 <strong><code>packages/index.js</code></strong> 注册与示例用法）。</li><li>本文档经用户审阅无修改后，再进入编码阶段。</li></ul>`,37)])])}const h=t(n,[["render",d]]);export{u as __pageData,h as default};
