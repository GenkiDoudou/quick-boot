# 异常处理模块设计

## 原则

- 业务失败 → `WarningException` / `ErrorException`
- 统一 `@RestControllerAdvice` → `R.fail`
- HTTP 200 + `code` 区分（与前端 `request.js` 约定一致）

## 错误码

`ErrorCodes` 枚举 + `HttpCodes`；OAuth 签名失败 **30002**。

## 使用文档

[异常处理模块使用文档](../../backend/components/通用组件/异常处理模块使用文档)
