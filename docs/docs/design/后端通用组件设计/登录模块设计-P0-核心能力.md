# 登录模块 P0：核心能力

- `POST /login`：用户名/密码 + 可选验证码
- Client HMAC 校验（除白名单）
- 成功返回 Token（Sa-Token）
- `GET /getInfo`：用户、角色、权限集合
- `GET /getRouters`：动态菜单
- `POST /logout`

前端：`permission.js` 路由守卫 + `user` store。
