# C7Radio 单选组

单选按钮组，API 与 `C7Checkbox` 类似（`dataList` / `fetchData`）。

**源码**：`quick-ui/src/packages/C7Radio/index.vue`

## 示例

```vue
<C7Radio v-model="form.sex" :data-list="sexOptions" />
```

Dev 页：`/dev/c7-radio-e2e`（菜单动态下发）。
