# DESIGN.md — 视觉设计规范

> AI 生成 UI 时请遵循以下 token，保证视觉一致。

## Design Tokens

| Token | 值 | 说明 |
|-------|-----|------|
| 主色 | `#409EFF` | 按钮、链接、高亮（Element Plus） |
| 成功色 | `#52c41a` | 成功状态 |
| 警告色 | `#faad14` | 警告状态 |
| 错误色 | `#ff4d4f` | 错误 / 危险操作 |
| 圆角 | `4px` | 卡片、按钮、输入框 |
| 间距基数 | `8px` | 使用 8 / 16 / 24 / 32 / 48 |
| 正文字号 | `14px` | 行高约 22px |
| 标题字号 | `20px / 16px / 14px` | h1 / h2 / h3 |

## 设计原则

- 优先使用 Element Plus 内置主题变量，避免一次性硬编码颜色
- 间距遵循 8px 网格
- 断点：移动端 `<768px` / 平板 `768-1024px` / 桌面 `>1024px`
- 动效：微交互 `150ms`，过渡 `300ms`，复杂动画 `500ms`
- 图标：默认使用组件库图标集，引入额外图标包前需确认