# 测试用例清单 — 结构骨架

以下为生成物应遵循的 Markdown 区块顺序与字段名；具体内容由 OpenSpec 工件推导。

```markdown
# {模块中文名}-测试用例清单

## 测试环境信息

- **测试登录地址**：{URL，占位或从用户/配置确认}
- **测试账号**： {账号/密码或占位}

## 模块信息

- **模块名称**：{中文名}
- **模块缩写**：{大写英文缩写，用于用例编号前缀}
- **模块页面地址**：`{前端路由，无则写「不适用」或 N/A}`
- **前端组件**：`{主要 .vue，未知则写待补充}`
- **后端接口路径**：`{基础路径或「见接口清单」}`
- **测试用例总数**：{数字，与下文用例条数一致}

---

## TC_{缩写}_001 - {登录相关标题}

**功能名称**：{模块中文名或「系统登录」}

**用例标题**：{如：验证使用合法账号成功登录并进入系统}

**前置条件**：
1. {未登录或浏览器无痕等}

**测试步骤**：
1. 打开登录页（地址：…）
2. 输入合法账号、密码
3. 点击登录

**预期结果**：
1. 登录成功，跳转至首页/约定落地页
2. 会话有效（token/cookie 存在，后续业务页面可访问）

---

## TC_{缩写}_002 - …

（其余用例同上结构：功能名称、用例标题、前置条件、测试步骤、预期结果；用例之间 `---` 分隔）

---

## 测试用例统计

### 按功能分类统计

| 功能模块 | 测试用例数量 |
|---------|------------|
| … | … |
| **总计** | **N** |

### 按测试类型分类统计

| 测试类型 | 测试用例数量 |
|---------|------------|
| 正向流程（主流程） | … |
| 异常流程 | … |
| … | … |
| **总计** | **N** |

---

## 备注

1. **数据来源**：本清单由 OpenSpec 变更 `{change-name}` 的 proposal / design / specs / tasks 推导；与实现不一致处以实现为准并迭代清单。
2. **数据准备**：…
```

## 编号与排序约定

- **第一条**：`TC_{缩写}_001` **必须为登录类**（有前端则账号密码登录；纯后端/过滤器类变更可为「带合法凭证访问接口」等同等级前置）。
- **其后**：按「页面加载 → 列表/查询 → 新增 → 编辑 → 删除 → 分页排序 → 校验与异常 → 权限与安全 → 边界与幂等」等业务顺序排列；无 UI 则按接口/行为主线排列。
- **缩写**：取自模块简称或变更 slug 的规范缩写（如 `YGJCXX`），保持全篇一致。
