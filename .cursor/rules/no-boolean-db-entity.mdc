---
description: 库表与持久化实体禁止使用 boolean；是否类字段用 CHAR(1)/String 的 0/1
globs: "**/entity/**/*.java,**/vo/**/*.java,**/db/**/*.sql,**/*schema*.sql,**/*ddl*"
alwaysApply: false
---

# 禁止 DB / 实体使用 boolean

## 规则

- 数据库列、`entity`、与表映射的 VO：**不要**使用 `boolean` / `Boolean` / `TINYINT(1)` 表达是否语义。
- 使用 `String` + `CHAR(1)`，取值 **`0`=否**、**`1`=是**（对齐 `status`、`del_flag`）。
- 判断：`"1".equals(x)`；写入前归一化为 `"0"` / `"1"`。

## 示例

```java
// ❌
private Boolean checkCaptcha;

// ✅
/** 0=否 1=是 */
private String checkCaptcha;
```

```sql
-- ❌
check_captcha TINYINT NOT NULL DEFAULT 0

-- ✅
check_captcha CHAR(1) NOT NULL DEFAULT '0'
```

配置类 / 与库无关的运行时开关（如 `qc.captcha.enabled`）不在本规则范围。
