# 项目协作指南（AGENTS.md）

本仓库是一个 **Spring Boot 3（后端）+ Vue 3 + Vite + Element Plus（前端）+ VitePress（文档）** 的全栈项目集合。请优先遵循仓库内已有规则文件：`.cursorrules`、`.cursor/rules/*`（含 **`.cursor/rules/karpathy-guidelines.mdc`**）与本文 **「Karpathy 编码行为准则」** 节。

## 文件编码规范（强制）

- 新建或修改任何文本文件时，**一律使用 UTF-8 无 BOM 编码**（强制）。
- 严禁使用 GBK/ANSI/UTF-16 等会导致跨环境乱码的编码。
- 严禁在源码文件头写入 BOM（`\ufeff`）；若编译报 `非法字符: '\ufeff'`，必须先移除 BOM 再提交。
- 若发现历史文件存在乱码或编码不一致，优先转换为 UTF-8 无 BOM 后再继续修改。
- 在 PowerShell 中写文件时，优先使用无 BOM 写法（如 `new UTF8Encoding($false)`）避免再次引入 `\ufeff`。

## `/brainstorming` 与需求澄清（本仓库约定）

当用户通过 **`/brainstorming`** 启动头脑风暴或设计类流程、且需要向用户澄清需求时：

- **在同一条回复中一次性列出**全部待澄清问题；**不要**采用「每次只问一个问题、等用户回复后再问下一题」的节奏。
- 每一道澄清题须使用**下列版式**（**按序号**逐题重复；选项字母与数量可按题意增减；**每题**须标出**一个**推荐项）：

```text
1. 问题: ……
选项:
A …… 
B …… 
C ……（推荐）
D: ……

2. 问题: ……
选项:  
A …… 
B ……（推荐）
```

说明：

- **`N. 问题:`**（`N` 为从 1 开始的序号）后接完整问句或澄清点描述。
- **`选项`** 单独一行；`选项` 与 **A** 之间至少一个空格；同一行内依次写 **A / B / C / D…** 及对应短语，选项之间用空格分隔；推荐项写在**该选项短语末尾**，用 **（推荐）** 或 **`(推荐)`** 标注均可。
- 若某题只需二选一，可仅写 **A … B …（推荐）** 等，不必凑满四个选项。
- 全部问题列完后，**必须**再给出**一行推荐汇总**（题号与上文序号对应；冒号后为该题推荐选项字母；多组之间用空格分隔），格式示例：

```text
推荐 1: A ,2: C , 3: B
```

必要时可在「推荐」行之后用一两句总述推荐理由。

若其他文档或技能（例如 brainstorming）写明「一次一问」，在本仓库执行 **`/brainstorming`** 时**以本节为准**（仅澄清问题的组织方式；设计定稿、未批准前不写实现代码等既有硬约束不变）。

## 问题修复与排障（强制）

当用户**提出问题并要求修复**（如报错、行为异常、回归、性能问题、「帮我修一下」「fix」「解决」等）时，**默认分两阶段**协作，**禁止**在未获确认前直接改项目源码或配置。

### 阶段一：诊断与方案（禁止改代码）

在**第一次**调用 `Write` / `StrReplace` / `EditNotebook` 等会改动仓库文件的工具之前，Agent **必须**完成：

1. **调查根因**：阅读相关代码与配置；必要时运行只读或复现类命令（日志、测试、构建、grep 等），说明依据而非猜测。
2. **输出诊断结论**：现象、根因（或最可能原因及待验证点）、影响范围。
3. **提出修改意见与方案**：给出可执行的修复思路；若存在多种做法，列出选项并标明**推荐项**及理由；说明风险、副作用与建议验证方式。
4. **等待用户确认**：明确请用户确认采用哪一方案（或补充约束）后，再进入阶段二。

阶段一回复中**不得**提交修复性 diff；仅允许为定位问题而做的只读操作。

### 阶段二：按确认实施

仅在用户**明确确认**方案之后（如「按推荐改」「用方案 B」「可以动手了」「直接改」等），方可修改代码。

- 若本次实施属于**非琐碎编码**（见下文 Karpathy 准则），在**第一次改文件前**仍须输出「假设 / 成功标准 / 最小改动计划」三块，且计划须与**已确认方案**一致，不得擅自扩大范围。
- 实施结束后简要说明验证情况（已跑命令、人工检查，或未验证及原因）。

### 例外（可跳过「先方案、后改码」）

- 用户在同一轮或上一轮已**明确批准**某修复方案，并指示「按上面方案实现 / 直接改 / Implement the plan」。
- 用户**仅**要求分析、评审、解释原因，**未**要求修改代码。
- 用户明确说「不用先出方案，直接改」。
- **琐碎修复**且用户已指明具体改法（如单处错别字、单行格式），改动可一眼核对、无歧义。

### 与其它流程的关系

| 场景 | 要求 |
|------|------|
| 问题修复（默认） | 先阶段一（根因 + 方案 + **等确认**），再阶段二 |
| OpenSpec `/opsx-apply`、Skill 要求连续落任务 | 若用户表述为「修复某问题」而非「按 tasks 实现」，仍以本节为准；已归档方案或用户已确认的设计除外 |
| Karpathy §5 | 在**已确认修复方案**后的阶段二、且为非琐碎编码时执行，不替代阶段一的等待确认 |

## 目录结构（高层）

- `quickboot/`：后端（Maven 多模块）
  - `quickboot-common/`：通用能力（工具类、通用响应、异常、安全组件等）
  - `quickboot-core/`：核心配置/基础设施能力
  - `quickboot-web/`：Web 启动模块（Controller/Service/Mapper、Spring Boot 启动类、资源配置）
- `quick-ui/`：前端（Vue 3 + Vite，使用 `pnpm`）
- `docs/`：文档站点（VitePress）
- `原始需求/`：由分析/梳理产出的“原始需求”文档（不参与构建）

## 环境与依赖

- 后端：JDK **17+**、Maven **3.6+**、MySQL **8+**（或按实际配置）
- 前端/文档：Node.js（建议 **18/20**）、`pnpm`（`quick-ui/package.json` 标注 `pnpm@9.0.0`）

## 常用命令

### 后端（`quickboot/`）

```bash
cd quickboot
mvn clean install -DskipTests

# 启动 web 模块（端口默认 9992）
mvn -pl quickboot-web spring-boot:run
```

### 前端（`quick-ui/`）

```bash
cd quick-ui
pnpm i
pnpm dev
```

### 文档（`docs/`）

```bash
cd docs
pnpm i
pnpm dev
```

## Karpathy 编码行为准则（强制）

降低 LLM 常见编码失误的行为约束；与 `.cursor/rules/karpathy-guidelines.mdc` 保持一致。**细则以该文件为准**；本节为协作总纲摘要，**Agent 与人类协作者均须严格遵守**，不得因 Plan 模式、OpenSpec、Skill 或「完成全部 to-do」等理由跳过。

**权衡：** 偏谨慎而非求快；**琐碎任务**可酌情简化（见下）。

### 协作执行（强制，优先于其它流程）

| 场景 | 要求 |
|------|------|
| 用户提出问题并要求**修复** | 先完成 **「问题修复与排障」** 阶段一（根因 + 方案 + **等确认**），**禁止**未确认即改代码；见上文专节 |
| 非琐碎编码（见下） | **同一条回复、第一次改文件之前**，必须先输出 §5 的「假设 / 成功标准 / 最小改动计划」三块 |
| 用户说「Implement the plan」/「按方案实现」 | 视为已确认方案；**不豁免** §5；须根据计划写出**本次实现**的最小步骤与范围，禁止把「对比方案」默认同「全量落库+前后端」 |
| 计划含「待确认」类 to-do（如 decide-tier、阈值、覆盖范围） | **先澄清或给出默认并写明假设**，不得静默替用户选定最重实现路径 |
| Cursor Plan 模式 | 仅调研与出计划，**不写**实现代码；退出 Plan 进入 Agent 实现时 **仍须** §5 |
| 与 OpenSpec / brainstorming / TDD 冲突 | **Karpathy §5 优先**（见 `.cursor/rules/karpathy-guidelines.mdc` §0） |

**实现前自检（Agent 必做，未通过则禁止调用写文件工具）：**

1. 预计是否改动 **>1 个文件** 或 **>~20 行**？→ 是则必须已输出 §5。
2. 用户是否明确「直接改 / 不用计划」？→ 否则不得跳过 §5。
3. 本次 diff 是否都能追溯到**用户当前请求**？→ 不得顺带实现计划里「可选/下一步」内容。
4. 是否存在更小可行方案（如仅配置、仅日志）？→ 须在 §5 中说明为何未采用。

**实现后：** 用一两句话对照 §5「成功标准」说明已验证项或未验证原因。

### 优先级与适用范围

**本准则始终生效。** 在非琐碎编码任务上，若与可选流程（如 brainstorming、writing-plans、TDD 等 Skill）冲突：

1. **先完成「写代码前强制输出」**（假设、成功标准、最小改动计划）。
2. 再按需执行 OpenSpec / 项目 Skill（用户明确要求或任务显然需要时）。
3. **禁止**为凑流程而扩大范围（额外重构、抽象、文档等），除非用户要求。

**琐碎任务**（可跳过强制输出块，直接执行）：错别字/格式修正、单行改动、纯问答/评审且不改文件、或用户明确说「直接改 / 不用计划」。

**非琐碎任务**（改任何项目文件前必须先输出强制块）：新功能、bug 修复、重构、配置/构建变更，或预计改动 **>1 个文件** 或 **>~20 行**。

### 1. 先想再写

- 明确写出假设；不确定则提问，不要猜。
- 存在多种理解时一并列出，不要静默选一种。
- 有更简单做法时指出；需求不清时停下并说明困惑点。

### 2. 简单优先

- 只解决用户提出的问题，不加未要求的功能。
- 不为只用一次的场景抽象；不加未要求的「灵活性/可配置性」。
- 不对不可能场景过度防御；200 行能 50 行搞定则重写。

### 3. 手术式改动

- 只改必须改的；不顺手「优化」相邻代码、注释或格式。
- 匹配现有风格；发现无关死代码可提及，不要擅自删除。
- 因本次改动产生的无用 import/变量须清理；**改动前**已存在的死代码不要动。
- 检验标准：**每一行 diff 都能追溯到用户请求**。

### 4. 目标驱动

- 把任务变成可验证目标（如：加校验 → 测无效输入；修 bug → 先复现再修；重构 → 前后测试通过）。
- 多步任务用「步骤 → 验证方式」列出计划。

### 5. 写代码前强制输出（仅非琐碎任务）

在**第一次修改项目文件之前**，于**同一条回复**中输出（默认简体中文，用户用英文交流时用英文）：

```text
### 假设
- …

### 成功标准
- …

### 最小改动计划
1. [步骤] → 验证: [如何确认]
2. …
```

要求：

- **必须**在本块完成后再调用写文件/改文件工具。
- 需求含糊时在「假设」中提问，或输出本块后等待确认——**禁止**猜测并实现。
- 计划保持最小（符合 §2、§3），仅覆盖用户请求所需步骤。
- 实现结束后简要说明哪些成功标准已验证（已跑命令、人工检查，或未验证及原因）。

**生效标志：** diff 更小、返工更少、澄清问题出现在实现之前而非之后。

## 代码与改动约定（面向协作/维护）

- **避免误扫/误改大目录**：不要对 `**/node_modules/**`、`bak/` 做递归检索或批量改动。
- **后端分层**：尽量保持 `controller / service / mapper / entity / dto / vo` 分层清晰。
- **前端组织**：API 放在 `quick-ui/src/api/`，通用组件放在 `quick-ui/src/components/`。

### 生成代码时必须引用的规范文档（AI / OpenSpec / 脚手架）

在**新建或生成**对应产物前，须**用读取工具打开并通读**下列规范**源文件**（路径相对仓库根目录）；禁止把规范正文抄进规则文件代替阅读：

- **项目级约束（技术栈、架构、流程、协作规范）**：`openspec/project.md`
- **后端（Java / Maven 等）**：`sdd/后端代码规范.md`
- **前端（Vue / TypeScript 等）**：`sdd/前端代码规范.md`
- **数据库（DDL、Flyway 迁移、表结构设计说明等）**：`sdd/数据库设计规范.md`

执行要求补充：
- 生成代码时必须落实 `openspec/project.md` 中的项目背景、技术栈、分层方式、验证流程与协作约束。
- 若 `openspec/project.md` 与其他规范文档存在冲突，优先级顺序为：`openspec/project.md` > 口头需求；并与用户确认例外。

### 前端设计规范补充（DESIGN.md）

在新建、生成或改造任何前端页面/组件（Vue、TS、样式）前，必须先读取仓库根目录 `DESIGN.md`，并按其中设计系统要求实现视觉与交互。

**列表页（C7JsonTable）**：还须阅读 `sdd/前端代码规范.md` 中「列表页样式与代码模板」，并对照同域已有页面（默认 **`views/system/config/index.vue`**）实现，避免单页自造布局或样式。

执行要求：
- 禁止将 `DESIGN.md` 正文复制到本文件或其他规则文件中替代阅读。
- 生成代码时必须落实 `DESIGN.md` 中的颜色、字体、层级、间距、状态样式与动效约束。
- 当现有页面改造为新风格时，优先抽取页面级 design token 并保持同一页面内一致。
- 若口头需求与 `DESIGN.md` 冲突，默认以 `DESIGN.md` 为准，并向用户确认例外。

### 生成 SQL / 前后端代码时的注释要求（AI / 脚手架 / 批量产出）

生成或脚手架产出 **SQL（含 Flyway 迁移）**、**后端 Java**、**前端 Vue/TS/JS** 时，**必须**编写**详细且符合项目标准**的注释；禁止提交无注释或仅重复标识符名的占位注释。

**通用原则**

- 注释语言以**简体中文**为主；对外 API 文档类注释可与 OpenAPI 描述一致。
- 重点说明**职责、业务含义、为什么这样写、边界条件与约束**；避免复述代码字面含义（如 `// 设置用户名`）。
- 与 `sdd/后端代码规范.md`、`sdd/前端代码规范.md`、`sdd/数据库设计规范.md` 中注释章节保持一致；冲突时以 `sdd/` 规范为准。

**SQL / DDL（Flyway 迁移、建表脚本）**

- 每个迁移文件头部用块注释说明：**变更目的、影响范围、依赖的前置版本或表**。
- **表**：`COMMENT ON TABLE` 或 MySQL `COMMENT` 说明业务用途；枚举/状态类字段须在注释中写明**取值含义**（可与历史脚本 `::` 分隔风格对齐）。
- **列**：每张业务表的每个字段均须有列注释；主键、外键、逻辑删除、审计字段须注明语义与默认值约定。
- **索引 / 约束**：非显而易见的索引须注释**查询场景或性能目的**；复杂 `ALTER` 须说明**数据迁移或回填策略**。

**后端 Java**

- **public** 类型及 **public/protected** 成员须具备标准 **JavaDoc**（`/** ... */`），包含：`@param`、`@return`（有返回值时）、`@throws`（有受检或业务异常时）、必要时 `@since`。
- **Controller** 方法：说明接口用途、权限/鉴权前提、幂等性与副作用；与 `@Operation` 描述互补而非重复空话。
- **Service / Mapper**：非显而易见的事务边界、批量处理、与外部系统交互、缓存与并发约束须注释。
- **DTO / VO / Entity 字段**：业务含义不直观的字段须 JavaDoc 或行内说明（枚举值、单位、可空性原因）。

**前端 Vue / TS / JS**

- 默认导出的**页面组件、业务组件、composable、api 模块函数**须具备标准 **JSDoc**，包含：`@param`、`@returns`、复杂类型用 `@typedef` 或 TS 类型导出说明。
- **api/** 封装：每个导出函数说明**对应后端路径、请求方法、主要入参与返回 data 结构**。
- **复杂逻辑**：表单校验规则、权限分支、与后端字段映射、非显而易见的 `watch`/`computed` 须注释**原因与边界**。
- **Vue SFC**：`<script>` 内模块级常量、关键 composable 调用处补充说明；模板中仅靠命名无法表达的交互须在上文 JSDoc 或邻近注释说明。

**验收**：生成物提交前自检——新增 public API、表/列、导出函数是否均具备可读注释；删改存量代码时同步更新相关注释，避免文档与实现脱节。

### 后端接口生成补充约束（Controller / Service）

- 默认不使用 `@PutMapping` / `@DeleteMapping`，统一优先 `@PostMapping` 表达修改/删除语义。
- 参数必须接入 Jakarta Validation。
- 对外 REST 接口至少包含 `@Tag`、`@Operation`、关键参数 `@Parameter`。
- 禁止抛出 `IllegalArgumentException` 作为业务失败信号，应使用项目自定义异常。
- **命名**：生成新接口时，路径段、查询参数、请求体/响应体 JSON 字段统一使用**驼峰命名（camelCase）**（如 `/getInfo`、`userName`）；禁止在新接口中使用下划线（`user_name`）或连字符（`user-name`）风格。DTO/VO 字段与前端 `api/` 封装入参、解构字段与之保持一致。

## 提交/验证建议（尽量局部、可复现）

- 后端优先模块级测试，再视情况全量。
- 前端至少执行一次 `pnpm build:prod`。
- 提交前核对依赖声明与代码引用一致。
