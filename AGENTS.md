# AGENTS.md

- 项目：quickboot
- 技术栈摘要：Vue 3 (quick-ui); uni-app (quick-h5); Maven Java 17 / Spring Boot 4.0.0 (quickboot); VitePress (docs)
- 是否 Monorepo：是
- 前端 (`quick-ui`)：Vue 3，UI=Element Plus，样式=Sass/SCSS，包管理=pnpm；语言以 JS 为主，可逐步 TypeScript
- 移动端 (`quick-h5`)：uni-app Vue3 + uView Pro（H5 / 微信小程序）
- 后端 (`quickboot`)：Maven，Java 17，Spring Boot 4.0.0；模块 `common` / `core` / `module-system` / `app`
- 文档 (`docs`)：VitePress

## 文档职责

| 文件 | 管什么 |
|------|--------|
| **本文件 `AGENTS.md`** | Agent 协作流程、排障、Karpathy、生成代码注释强制要求、Never/多 Agent 安全、构建命令、快捷指令 |
| **`code_formater.md`** | 编码 / 库表 / Maven·Modulith 分层 / 前后端约定 / 命名 / 注释细则（**写代码时以它为准**） |
| **`DESIGN.md`** | 前端视觉 token |
| **`.cursor/rules`** | 可自动注入的硬规则（如布尔字段、Karpathy 细则） |

写或改业务代码、DDL、前端页面之前，**须通读** `code_formater.md` 相关章节。冲突时：协作流程以本文件为准；编码事实以 `code_formater.md` + `.cursor/rules` + 现网为准。

## 生成代码注释（强制）

Agent **生成或实质性改写**前端 / 后端业务代码时，**必须**同步补充**详细、可维护的中文注释**（细则与示例以 `code_formater.md`「注释」相关章节为准）。禁止只交「能跑的裸代码」。

适用范围：`quick-ui`、`quick-h5`、`quickboot`（及同仓库其它业务源码）；**新建文件**与**本次新增/改写的公开 API、复杂逻辑**均须覆盖。

### 最低要求

| 侧 | 必须注释 |
|----|----------|
| **后端** | 类用途；public/protected 方法的职责、关键参数、返回值、副作用/事务；Entity/VO/DTO 字段业务含义（字典列标明类型编码）；非显而易见的分支、并发、缓存、SQL 意图 |
| **前端** | 页面/组件职责；composable / 关键工具函数的入参与返回；复杂 `watch`/`computed`/请求编排的触发条件与边界；非字面可读的业务规则、状态机、权限前提 |
| **通用** | 注释写「做什么 / 为什么 / 边界与约束」，禁止复述标识符的无信息废话；语言默认**简体中文** |

### 豁免（可少写，但不得整文件零注释）

- 纯格式/重命名/错别字等琐碎改动，且未改行为。
- 仅改配置、文案、样式数值且逻辑未变。
- 用户明确说「不用写注释 / 少写注释」。

未满足本节即视为实现不完整，须在交付前补齐后再宣称完成。

## 项目结构

```
quickboot/
|-- quick-ui/   # 前端（管理端）
|-- quick-h5/   # 前端（uni-app H5 / 微信小程序）
|-- quickboot/   # Maven 模块: quickboot-common, quickboot-core, quickboot-module-system, quickboot-app
|-- docs/   # VitePress
`-- ...
```

- 禁止编辑框架自动生成缓存目录（若存在：`.next/`、`.nuxt/`、`.umi/`、`.angular/`）

## `/brainstorming` 与需求澄清

当用户通过 **`/brainstorming`** 启动头脑风暴或设计类流程、且需要向用户澄清需求时：

- **在同一条回复中一次性列出**全部待澄清问题；**不要**采用「每次只问一个问题、等用户回复后再问下一题」的节奏。
- 每一道澄清题须使用下列版式（按序号逐题重复；选项字母与数量可按题意增减；**每题**须标出**一个**推荐项）：

```text
1. 问题: ……
选项:
A ……
B ……
C ……（推荐）
D: ……

2. 问题: ……
选项:
A ……
B ……（推荐）
```

说明：

- **`N. 问题:`** 后接完整问句或澄清点描述。
- **`选项`** 单独一行；同一行内依次写 A / B / C / D…；推荐项写在该选项短语末尾，用 **（推荐）** 标注。
- 若某题只需二选一，可仅写两个选项，不必凑满四个。
- 全部问题列完后，**必须**再给出**一行推荐汇总**，格式示例：

```text
推荐 1: A ,2: C , 3: B
```

必要时可在「推荐」行之后用一两句总述推荐理由。

若其他文档或技能写明「一次一问」，执行 **`/brainstorming`** 时**以本节为准**（仅澄清问题的组织方式；设计定稿、未批准前不写实现代码等既有硬约束不变）。

## 问题修复与排障（强制）

当用户**提出问题并要求修复**（如报错、行为异常、回归、性能问题、「帮我修一下」「fix」「解决」等）时，**默认分两阶段**协作，**禁止**在未获确认前直接改项目源码或配置。

### 阶段一：诊断与方案（禁止改代码）

在**第一次**调用 `Write` / `StrReplace` / `EditNotebook` 等会改动仓库文件的工具之前，Agent **必须**完成：

1. **调查根因**：阅读相关代码与配置；必要时运行只读或复现类命令，说明依据而非猜测。
2. **输出诊断结论**：现象、根因（或最可能原因及待验证点）、影响范围。
3. **提出修改意见与方案**：给出可执行修复思路；多种做法时列出选项并标明**推荐项**及理由；说明风险与验证方式。
4. **等待用户确认**：明确请用户确认采用哪一方案后再进入阶段二。

阶段一回复中**不得**提交修复性 diff；仅允许为定位问题而做的只读操作。

### 阶段二：按确认实施

仅在用户**明确确认**方案之后（如「按推荐改」「用方案 B」「可以动手了」「直接改」等），方可修改代码。

- 若本次实施属于**非琐碎编码**（见下文 Karpathy 准则），在**第一次改文件前**仍须输出「假设 / 成功标准 / 最小改动计划」三块，且计划须与**已确认方案**一致，不得擅自扩大范围。
- 实施结束后简要说明验证情况（已跑命令、人工检查，或未验证及原因）。

### 例外（可跳过「先方案、后改码」）

- 用户已**明确批准**某修复方案，并指示「按上面方案实现 / 直接改 / Implement the plan」。
- 用户**仅**要求分析、评审、解释原因，**未**要求修改代码。
- 用户明确说「不用先出方案，直接改」。
- **琐碎修复**且用户已指明具体改法（如单处错别字、单行格式），改动可一眼核对、无歧义。

### 与其它流程的关系

| 场景 | 要求 |
|------|------|
| 问题修复（默认） | 先阶段一（根因 + 方案 + **等确认**），再阶段二 |
| OpenSpec `/opsx-apply`、Skill 连续落任务 | 若表述为「修复某问题」而非「按 tasks 实现」，仍以本节为准；已确认设计除外 |
| Karpathy §5 | 在**已确认修复方案**后的阶段二、且为非琐碎编码时执行，不替代阶段一的等待确认 |

## Karpathy 编码行为准则（强制）

降低 LLM 常见编码失误的行为约束；若存在 `.cursor/rules/karpathy-guidelines.mdc`，**细则以该文件为准**。本节为协作总纲摘要，**须严格遵守**，不得因 Plan 模式、OpenSpec、Skill 或「完成全部 to-do」等理由跳过。

**权衡：** 偏谨慎而非求快；**琐碎任务**可酌情简化。

### 协作执行（强制，优先于其它流程）

| 场景 | 要求 |
|------|------|
| 用户提出问题并要求**修复** | 先完成「问题修复与排障」阶段一，**禁止**未确认即改代码 |
| 非琐碎编码 | **同一条回复、第一次改文件之前**，必须先输出 §5「假设 / 成功标准 / 最小改动计划」 |
| 「Implement the plan」/「按方案实现」 | 视为已确认方案；**不豁免** §5；只实现已确认的最小范围 |
| 计划含「待确认」类 to-do | **先澄清或给出默认并写明假设**，不得静默选最重路径 |
| Cursor Plan 模式 | 仅调研与出计划，**不写**实现代码；进入实现时 **仍须** §5 |
| 与 OpenSpec / brainstorming / TDD 冲突 | **Karpathy §5 优先** |

**实现前自检（未通过则禁止写文件）：**

1. 预计是否改动 **>1 个文件** 或 **>~20 行**？→ 是则必须已输出 §5。
2. 用户是否明确「直接改 / 不用计划」？→ 否则不得跳过 §5。
3. 本次 diff 是否都能追溯到**用户当前请求**？→ 不得顺带做可选/下一步。
4. 是否存在更小可行方案？→ 须在 §5 中说明为何未采用。

**实现后：** 用一两句话对照「成功标准」说明已验证项或未验证原因。

### 优先级与适用范围

**本准则始终生效。** 在非琐碎编码任务上，若与可选流程冲突：

1. 先完成「写代码前强制输出」（假设、成功标准、最小改动计划）。
2. 再按需执行 OpenSpec / 项目 Skill。
3. **禁止**为凑流程而扩大范围，除非用户要求。

**琐碎任务**（可跳过强制块）：错别字/格式修正、单行改动、纯问答/评审且不改文件、或用户明确说「直接改 / 不用计划」。

**非琐碎任务**（改文件前必须输出强制块）：新功能、bug 修复、重构、配置/构建变更，或预计改动 **>1 个文件** 或 **>~20 行**。

### 1. 先想再写

- 明确写出假设；不确定则提问，不要猜。
- 存在多种理解时一并列出，不要静默选一种。
- 有更简单做法时指出；需求不清时停下并说明困惑点。

### 2. 简单优先

- 只解决用户提出的问题，不加未要求的功能。
- 不为只用一次的场景抽象；不加未要求的「灵活性/可配置性」。
- 不对不可能场景过度防御；200 行能 50 行搞定则重写。

### 3. 手术式改动

- 只改必须改的；不顺手「优化」相邻代码、注释或格式。
- 匹配现有风格；发现无关死代码可提及，不要擅自删除。
- 因本次改动产生的无用 import/变量须清理；**改动前**已存在的死代码不要动。
- 检验标准：**每一行 diff 都能追溯到用户请求**。

### 4. 目标驱动

- 把任务变成可验证目标。
- 多步任务用「步骤 → 验证方式」列出计划。

### 5. 写代码前强制输出（仅非琐碎任务）

在**第一次修改项目文件之前**，于**同一条回复**中输出（默认简体中文）：

```text
### 假设
- …

### 成功标准
- …

### 最小改动计划
1. [步骤] → 验证: [如何确认]
2. …
```

要求：

- **必须**在本块完成后再调用写文件/改文件工具。
- 需求含糊时在「假设」中提问，或输出本块后等待确认——**禁止**猜测并实现。
- 计划保持最小，仅覆盖用户请求所需步骤。
- 实现结束后简要说明哪些成功标准已验证。

## 构建与开发命令

### 前端 (quick-ui)
```bash
pnpm dev  # vite
pnpm run build  # vite build
```

路径别名: @/* -> src/*

### 移动端 (quick-h5)
```bash
cd quick-h5
pnpm install   # 或 npm install --legacy-peer-deps
pnpm dev:h5
pnpm build:mp-weixin
```

详见 `quick-h5/README.md`。

### 后端 (quickboot)
```bash
mvn clean install -DskipTests
mvn -pl <module> spring-boot:run
```

### 文档 (docs)
```bash
cd docs
pnpm i
pnpm dev
```

## Never 规则

- 禁止提交密钥、token 或 `.env` 内容
- 禁止修改 `node_modules/`、构建产物（`dist/`、`build/`、`target/`）或 `.git` 内部
- 禁止静默整文件覆盖手写维护的 `AGENTS.md` / `DESIGN.md` / `code_formater.md`；应使用 suggested 或先征得确认
- 非琐碎多文件改动前，禁止跳过假设 / 成功标准 / 最小计划
- 写业务代码前未通读 `code_formater.md` 相关章节即开写 —— 禁止
- 生成前后端功能时未检索/复用现有组件与工具类、平行再造 —— 禁止（细则见 `code_formater.md` §0）
- 生成或实质性改写前后端业务代码时不写详细中文注释 —— 禁止（见上文「生成代码注释」与 `code_formater.md`）
- 编码红线（布尔字段、`R<T>`、OpenAPI、禁止页面裸调 axios 等）以 `code_formater.md` 与 `.cursor/rules` 为准，不得以「AGENTS 里没写细」为由违反

## 多 Agent 安全

- 禁止擅自 `git stash`、禁止擅自切分支
- 修改全局配置或安装依赖前先与用户确认
- 改动保持手术式，禁止顺手大重构
- 只提交自己改动的文件；Conventional Commits（格式见 `code_formater.md`）

## 快捷指令

| 指令 | 说明 |
|------|------|
| 分析项目规范 | 扫描源码隐性约定，提出补丁（禁止静默覆盖 AGENTS.md） |
| CR 代码 | 按 Never 规则与 `code_formater.md` Review，并起草 Conventional Commit |
| 生成变量名 | 推荐变量 / 函数 / 组件 / CSS 类名（命名见 `code_formater.md`） |
| 合并 suggested | 对比 `*.suggested.md` 与正式文件，仅合并事实层（需用户确认） |
| 记录 correction | 向 `.agents/logs/corrections.md` 追加一行 |

## 规范路径索引

- `.cursor/rules`
- `code_formater.md`（编码事实源）
- `DESIGN.md`（前端视觉）
- `openspec/changes/spring-modulith-maven-layering/`（Modulith / 新域模板；归档后跟主 specs）

## 协作摘要

- 写代码：先读 `code_formater.md`（含 §0 优先复用）；生成代码须带详细中文注释（见「生成代码注释」）
- 问题修复：先诊断并给出方案，用户确认后再改代码（用户明确要求直接改除外）
- 非琐碎编码：改文件前先写清假设、成功标准、最小计划
