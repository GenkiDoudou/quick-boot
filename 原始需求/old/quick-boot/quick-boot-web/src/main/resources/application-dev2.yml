spring:
  datasource:
    username: root
    password: MYSQL@luyanan0718
    url: jdbc:mysql://14.29.233.185:13306/quick_boot?characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=false&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=GMT%2B8&allowMultiQueries=true&allowPublicKeyRetrieval=true&autoReconnect=true&failOverReadOnly=false
    driver-class-name: com.mysql.cj.jdbc.Driver
logging:
  level:
    com.su60.quickboot: debug

# 加密配置示例
# 复制到 application.yml 或 application-dev.yml 中使用

security:
  encryption:
    enabled: true  # 总开关
    request:
      enabled: true  # 请求加密开关
      algorithm: AES  # 加密算法：AES/RSA
      key-exchange: RSA  # 密钥交换方式：RSA
    response:
      enabled: true  # 响应加密开关
      algorithm: AES
    signature:
      enabled: true  # 签名验证开关
      algorithm: HMAC-SHA256  # 签名算法：HMAC-SHA256/RSA-SHA256
      secret-key: ${ENCRYPTION_SECRET_KEY:qc2026}  # HMAC密钥（环境变量，默认值）
      expire-time: 300  # 签名有效期（秒）
    ignore-urls:  # 忽略加密/签名的URL

